{
  "name": "auto-signature",
  "version": "1.0.0",
  "description": "Sistema para assinatura automática para o email com dados do protheus",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "activedirectory": "^0.7.2",
    "dotenv": "^16.5.0",
    "express": "^5.1.0"
  }
}
